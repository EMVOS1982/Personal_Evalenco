name: Deploy of server #nombre del workflow

on:
  push:
    branches: [ master ]

jobs:
  first_job:
    name: Tutorial codigo facilito
    runs-on: ubuntu-latest   # ruunners en que sistema operativo de la maquina virutal se correra

    env:
     NAME: CodigoFacilito #este seria el nombr del job 

    steps:									#tareas a realizar que estan en los job 
      - name: Working dir
        run: ls -al
    ## las acctiones se utilizan a nivel de step   
      - name: acciones
        uses: action/chekout@v4     #consta de 3 partes nombre del propriario/nombre de la acction y por ulitmo la version a usar anteponiendo una @ en este caso el accgtion clona la rama 
        #whit:  #permite recibir parametros para usar la acction
         #php-version: ${{ matrix.php}}
         #tools: composer
         #coverage: none


      - name: working dir affter acction
        run: ls -al

  secund_job:
   needs: first_job         # indica que depende de otro job en este caso first_job, lo cual indica que se ejecutar despues de finalizada su dependencia
   runs-on: ubuntu-latest

   steps:
    - name: paula  valencia
      run: echo "hola Paula valencia ${{github.ref}}"
